## Backend docs

Historical HTML schema/UI snapshots were removed to avoid drifting from the live, dynamic filter system.

- **Database reference**: `buildProHQ_Backend/database/README.md`
- **Source of truth (runtime)**: TypeORM entities + migrations in `buildProHQ_Backend/buildprohq-api/src/infrastructure/persistence/typeorm/`

