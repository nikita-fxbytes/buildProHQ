## Frontend docs

Legacy static HTML snapshots were removed to keep documentation aligned with the live, dynamic filter system.

- **Run the app**: `npm run dev`
- **Key dynamic pages**:
  - `/super/analytics` (project-scoped, dynamic filters)
  - `/super/filters` (dynamic project filters CRUD)

